const { userModels } = require("@/models/userModels/userModels");

const inputFields = userModels;

const outputFields = {};

const inputOutputFields = {
  inputFields,
  outputFields,
};

module.exports = {
  inputFields,
  inputOutputFields,
  outputFields,
};
