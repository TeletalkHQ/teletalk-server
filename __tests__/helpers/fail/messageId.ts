import { FailTestExecutor } from "$/types";

//TODO Add message id fail tests
const messageIdFailTest: FailTestExecutor = () => {
  logger.debug("Add message id fail tests");
};

export { messageIdFailTest };
