import { failCollection } from "$/tests/integration/helpers/fail";
import { successCollection } from "$/tests/integration/helpers/success";

const testHelperCollection = {
  fail: failCollection,
  success: successCollection,
};

export { testHelperCollection };
